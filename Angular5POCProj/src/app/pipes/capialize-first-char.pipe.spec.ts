import { CapializeFirstCharPipe } from './capialize-first-char.pipe';

describe('CapializeFirstCharPipe', () => {
  it('create an instance', () => {
    const pipe = new CapializeFirstCharPipe();
    expect(pipe).toBeTruthy();
  });
});
